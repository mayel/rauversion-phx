
<div class="">
    <%= case @kind do 
      :published -> live_component(
                  RauversionExtension.UI.EventsLive.EventsListComponent,
                  id: "all-posts-published",
                  page: 1,
                  current_user: assigns[:current_user]
                )

      :mine -> live_component(
                  RauversionExtension.UI.EventsLive.UserEventsListComponent,
                  id: "all-draft-posts-published",
                  page: 1,
                  current_user: assigns[:current_user]
                )
      _ -> "no match on #{@kind}"
    end  %>
</div>